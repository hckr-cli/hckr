[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "devd"
dynamic = ["version"]
description = ''
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
keywords = []
authors = [
  { name = "Ashish Patel", email = "ashishpatel0720@gmail.com" },
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.8",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
]
dependencies = [
  "click",
]

[project.urls]
Documentation = "https://github.com/pateash/devd#readme"
Issues = "https://github.com/pateash/devd/issues"
Source = "https://github.com/pateash/devd"

# to make sure the default env is in same repo ( for Intellij to refer )
[tool.hatch.envs.default]
type = "virtual"
path = ".venv"

[project.scripts]
devd = "devd.cli:cli"

[tool.hatch.version]
path = "src/devd/__about__.py"

[tool.hatch.envs.types]
extra-dependencies = [
  "mypy>=1.0.0",
]
[tool.hatch.envs.types.scripts]
check = "mypy --install-types --non-interactive {args:src/devd tests}"

[tool.coverage.run]
source_pkgs = ["devd", "tests"]
branch = true
parallel = true
omit = [
  "src/devd_cli/__about__.py",
]

[tool.coverage.paths]
devd = ["src/devd", "*/devd/src/devd"]
tests = ["tests", "*/devd/tests"]

[tool.coverage.report]
exclude_lines = [
  "no cov",
  "if __name__ == .__main__.:",
  "if TYPE_CHECKING:",
]

## STYLING ( commenting in favour of `hatch fmt`
# creating an env for styling and use it to fix
[tool.hatch.envs.style]
dependencies = [
  "black"
]
[tool.hatch.envs.style.scripts]
lint = "black --check src/ tests/"
fix = "black -v src/ tests/"
